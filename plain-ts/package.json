{
  "name": "oms-fe-plain-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Plain-TS implementation of the open-mSupply stocktakes vertical, built from the reverse spec.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
