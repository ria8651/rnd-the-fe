name: oms_fe
description: "open-mSupply frontend rewrite — Flutter web implementation of the reverse spec."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  graphql: ^5.2.1
  go_router: ^14.6.2
  intl: ^0.20.1
  uuid: ^4.5.1
  shared_preferences: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
